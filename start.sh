# #!/bin/sh

# echo "[SSH] Starting SSH daemon..."
# /usr/sbin/sshd -D &  # Run SSH daemon in the background

# echo "[Foundry] Starting Foundry VTT..."
# exec /entrypoint.sh "$@"  # Start Foundry VTT using its original entrypoint
